<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

	<!-- toolbar_common -->
	<include layout="@layout/appbar_bof_route"/>

	<!-- body -->
	<include layout="@layout/body_bof_route"/>

	<!-- button -->
	<Button
		android:id="@+id/btn_bof_route_select"
		android:layout_width="match_parent"
		android:layout_height="48dp"
		android:layout_gravity="center|bottom"
		android:layout_margin="0dp"
		android:text="@string/next"
		android:textAllCaps="true"
		android:enabled="true"
		android:background="@drawable/button_bottom_enable_disable"
		style="@style/txt_opensans_eb_16_white">
	</Button>

</androidx.coordinatorlayout.widget.CoordinatorLayout>