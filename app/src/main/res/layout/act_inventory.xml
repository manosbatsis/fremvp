<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    android:background="@color/pale_gray"
    tools:context=".ui.inventory.InventoryActivity">

	<!-- toolbar_common -->
	<include layout="@layout/appbar_inventory"/>
	<include layout="@layout/body_inventory_init"/>

	<LinearLayout
        android:layout_width="match_parent"
		android:layout_height="48dp"
		android:orientation="horizontal"
		android:layout_gravity="bottom"
		android:visibility="visible">
		<Button
			android:id="@+id/btn_view_all_your_inventory"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:background="@drawable/button_bottom_enable_disable"
			android:gravity="center"
			android:text="@string/your_inventory_btn_view_all_your_inventory"
			style="@style/txt_opensans_b_16_white"/>
	</LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>