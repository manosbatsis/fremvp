package com.cyberlogitec.freight9.ui.inventory.progressview

/**  OnProgressChangeListener is an interface for listening to the appbar_progress_sof is changed. */
interface OnProgressChangeListener {
  /** called when the appbar_progress_sof is changed. */
  fun onChange(progress: Float)
}
